= form_for @cw_tool do |f|
  - if @cw_tool.errors.any?
    #error_explanation
      %h2= "#{pluralize(@cw_tool.errors.count, "error")} prohibited this cw_tool from being saved:"
      %ul
        - @cw_tool.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
